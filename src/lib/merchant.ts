/**
 * Реквізити мерчанта для платіжної системи (WayForPay).
 * ─────────────────────────────────────────────────────────────
 * ⚠️ ЗАПОВНИТИ ПЕРЕД ПОДАННЯМ ЗАЯВКИ. WayForPay перевіряє, що на сайті
 * є повне найменування, ІПН, юридична та фактична адреси, телефон і email.
 * Усі сторінки /oferta, /contacts, /refund читають значення звідси —
 * заповнюєш тут один раз, і воно зʼявляється скрізь.
 */
export const MERCHANT = {
  /** Повне найменування: «Фізична особа-підприємець Прізвище Імʼя По батькові» */
  legalName: '',
  /** ІПН / РНОКПП */
  taxId: '',
  /** Юридична адреса */
  legalAddress: '',
  /** Фактична адреса (де надається послуга) */
  actualAddress: '',
  /** Телефон у міжнародному форматі */
  phone: '',
  /** Email для звернень */
  email: '',
  /** Виписка з ЄДР: номер і дата запису (не обовʼязково, але пришвидшує модерацію) */
  edr: '',
} as const;

/** Домен, який подається у WayForPay як «магазин». */
export const SHOP_URL = 'https://teniss.vercel.app';
export const SHOP_NAME = 'DRUID BATTLE CUP';

/** Що саме продаємо — послуга, не товар. Для розділу «доставка». */
export const PRODUCT = {
  title: 'Участь у турнірі з настільного тенісу DRUID BATTLE CUP',
  price: 420,
  priceSupport: 840,
  currency: 'UAH',
  /** Дата надання послуги */
  eventDate: '13 вересня 2026',
  venue: 'простір ДРУЇД, м. Івано-Франківськ',
} as const;

export const filled = (v: string) => v.trim().length > 0;
