import type { Metadata } from 'next';
import LigaNav from '@/components/liga/LigaNav';
import CtaSmart from '@/components/liga/CtaSmart';
import '../liga.css';

export const metadata: Metadata = {
  title: 'Правила — DRUID BATTLE CUP',
  description: 'Регламент ліги: 21 день, 8 матчів, залік 50/50, День Х 23 серпня.',
};

/** Публічні правила ліги (з REGLAMENT-LIGY.md, D-036). */
export default function PravylaPage() {
  return (
    <main className="jn-root"><div className="jn-wrap wide">
      <LigaNav />

      <h1 className="jn-h1">Правила</h1>
      <p className="jn-lead">Коротко і чесно. Спірне вирішують орги більшістю.</p>

      <div className="jn-card">
        <h3>🗓 Як влаштовано</h3>
        <p className="jn-hint" style={{ margin: 0 }}>
          Реєстрація до <b>2 серпня</b> → <b>жеребкування 2.08</b> → ліга <b>2–23 серпня (21 день)</b> →
          <b> День Х — неділя 23 серпня</b>: фінали + міні-ігри. Все на ДРУЇДІ, стіл один — і цього достатньо.
        </p>
      </div>

      <div className="jn-card">
        <h3>🏓 Матчі ліги</h3>
        <p className="jn-hint" style={{ margin: 0 }}>
          У кожного — <b>8 обовʼязкових матчів</b>. Пари призначає система за рівнем; телеграм суперника — у профілі.
          Домовились — прийшли на ДРУЇД — зіграли. Матч = <b>до 2 перемог у сетах</b> (сет до 11, від 10:10 — різниця 2).
          За взаємною згодою ДО матчу — один сет до 21. Дедлайн усіх матчів: <b>22 серпня, 23:59</b>.
        </p>
      </div>

      <div className="jn-card">
        <h3>🤝 Чесність</h3>
        <p className="jn-hint" style={{ margin: 0 }}>
          Рахунок вносить будь-хто з двох; суперник бачить і може оскаржити протягом 24 год.
          Після матчу кожен відмічає, чи грав суперник на свій заявлений рівень (+1/+2/+3).
          Кілька «+3» від різних людей → розбір і можливий перевід у іншу категорію.
          Незіграний матч → обом 0:0; доведене «морозиво» → технічна перемога 1:0.
        </p>
      </div>

      <div className="jn-card">
        <h3>🧮 Очки — 50/50</h3>
        <p className="jn-hint" style={{ margin: 0 }}>
          Спортивні: перемога 3 · зіграна поразка 1 · незіграний 0.
          Фанові: бали міні-ігор Дня Х. Фінальний залік = <b>50% спорт + 50% фан</b> —
          тому шанс на топ має і середнячок. Рейтинг живе онлайн увесь час.
        </p>
      </div>

      <div className="jn-card">
        <h3>🏆 День Х і призи</h3>
        <p className="jn-hint" style={{ margin: 0 }}>
          Фінали топів наживо + міні-ігри для всіх. <b>Два головні призи</b> + відзнаки за заслугами кожному, як у марафоні.
          Зальотні без реєстрації — фан-частина за окрему оплату, без заліку.
        </p>
      </div>

      <CtaSmart />
    </div></main>
  );
}
